import { isPlainObject } from "@vuepress/shared";
import { pwaPlugin } from "vuepress-plugin-pwa2";
export const getPWAPlugin = (options, favicon, legacy = false) => {
    if (!options)
        return null;
    return pwaPlugin({
        ...(favicon ? { favicon } : {}),
        ...(isPlainObject(options) ? options : {}),
    }, legacy);
};
//# sourceMappingURL=pwa.js.map